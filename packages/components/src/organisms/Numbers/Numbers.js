import React from "react";

const variants = {
  simple: "",
  shadow: "",
  stroke: "border rounded-lg",
};
const Numbers = ({ number, title, iconName, description, variant, color }) => {
  return <div className="flex flex-col">Numbers</div>;
};
export default Numbers;
